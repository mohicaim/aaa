name: Run work

permissions:
  actions: write 
env:
  SYNCTOKEN: ${{ secrets.SYNCTOKEN }} 
  
  TOKEN: ${{ secrets.TOKEN }} 
  
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
  DATA_PASS: ${{ secrets.DATA_PASS }}      
          
  GH_URL: ${{ secrets.GH_URL }} 
  
  EPASS: ${{ secrets.EPASS }} 
           
on:
  #schedule:
    #- cron: "5 16 * * *"
  repository_dispatch:
    types: [mksha_trigger]
    
  workflow_dispatch: # 支持手动点击触发
 
jobs:
  run-service:
    runs-on: ubuntu-latest

    steps:
    
    
    
      # 🌟 新增这一步：捕获真正的任务开始时间，并写入全局环境变量
      - name: Record Real Start Time
        run: |
          echo "REAL_START_TIME=$(date +%s)" >> $GITHUB_ENV
   
         # 2. 登录到 Docker Hub (关键步骤)
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: mohicai
          password: ${{ secrets.DOCKERPULL }}  

   
      - name: Cleanup Disk Space (Free up to 30GB+)
        run: |
          echo "=== Before Cleanup ==="
          
          SIZE=$(df -B1 / | awk 'NR==2 {print $2}') 
          
          if [ "$SIZE" -gt $((100*1024*1024*1024)) ]; then 
          
            echo "大于100GB" 
            
          else 
          
            echo "小于100GB" 
            
            # 删除不需要的系统软件和工具链
            # 5gb
            sudo rm -rf /usr/share/dotnet
          
            # 10g
            sudo rm -rf /usr/local/lib/android
          
            sudo rm -rf /opt/ghc
          
          
            sudo rm -rf /usr/local/share/boost
          
            # 6gb
            sudo rm -rf "$AGENT_TOOLSDIRECTORY"
    
            # 彻底清理 Docker 缓存和残留镜像
            docker system prune -a -f --volumes
    
            echo "=== After Cleanup ==="
            #df -h /
            
          fi
    
          

    
    
      # 1. 签出仓库 B
      - name: Checkout Repo B
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.SYNCTOKEN }} # 记得去 Settings 开启 Read and write permissions
          
    
      # 【核心安全机制】判断当前运行的仓库是否为主仓库
      - name: Check and Sync Repository
      
        run: |
          echo "当前运行的仓库是: ${{ github.repository }}"
          
          # ==================================================
          # 1. 🛡️ 安全防御：如果是主仓库本身，直接退出
          # ==================================================
          if [ "${{ github.repository }}" = "mohicaim/aaa" ]; then
            echo "=================================================="
            echo "🛡️ 安全防御：检测到当前正是【主仓库本身】，无需执行同步！"
            echo "正在安全退出..."
            echo "=================================================="
            exit 0
          fi

          echo "=================================================="
          echo "🎯 确认身份：当前为【B 仓库】，开始检查更新..."
          echo "=================================================="
        
          # 配置 Git 机器人信息
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 2. 绑定主仓库并拉取最新状态
          git remote add upstream https://github.com/Metamask-ctrl/alist.git
          git fetch upstream
          
          # ==================================================
          # 3. 🔍 核心判定：对比本地与主仓库的 Commit ID
          # ==================================================
          LOCAL_COMMIT=$(git rev-parse HEAD)
          UPSTREAM_COMMIT=$(git rev-parse upstream/main)
          
          echo "当前 B 仓库最新 Commit: $LOCAL_COMMIT"
          echo "主仓库 A 最新 Commit:   $UPSTREAM_COMMIT"
          
          if [ "$LOCAL_COMMIT" = "$UPSTREAM_COMMIT" ]; then
            echo "=================================================="
            echo "☕ 检查完毕：当前已经是最新版本，无任何更新，无需同步。"
            echo "=================================================="
            exit 0 # 正常结束任务，不执行后面的同步代码
          fi

          # ==================================================
          # 4. 🚀 执行同步：只有不一致时才会走到这里
          # ==================================================
          echo "=================================================="
          echo "📢 检测到主仓库有新更新！正在强行同步..."
          echo "=================================================="
          
          # 强制覆盖本地 main 分支
          git checkout main
          git reset --hard upstream/main
          
          # 推送到自己的仓库 B（需配合你在上一步配置的 MY_SYNC_TOKEN）
          git push origin main --force
          
          echo "=================================================="
          echo "🎉 恭喜！新更新已成功同步到当前 B 仓库！"
            
          #重新触发当前任务
          
          HTTP_STATUS=$(curl -s -o response.json -w "%{http_code}" -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $SYNCTOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/mksha8721/abb/dispatches" \
              -d '{"event_type": "mksha_trigger"}')

          echo "GitHub API 响应状态码: $HTTP_STATUS"
              
          # GitHub 触发成功的标准状态码是 204
          if [ "$HTTP_STATUS" -eq 204 ]; then
            echo "=================================================="
            echo "🎉【触发成功】API 请求已成功送达，目标工作流已启动！"
            echo "=================================================="
            cat response.json 2>/dev/null || true
          else
            echo "=================================================="
            echo "❌【触发失败】API 请求返回异常！请检查 Token 或 URL 配置。"
            echo "错误响应内容如下："
            echo "=================================================="
            cat response.json
                exit 1 # 触发失败时，强行中断并让当前 Action 报错显红
          fi
          
          #退出当前任务
          #gh run cancel ${{ github.run_id }}
          exit 1

          
      # ================= 【缓存优化 1：APT 包管理器缓存】 =================
      - name: Cache APT Packages
        id: cache-apt
        uses: actions/cache@v4
        with:
          path: /var/cache/apt/archives
          key: ${{ runner.os }}-apt-rclone-fuse3-expect
          
        
      # === 依赖安装（原步骤） ===
      - name: Parallel Install Dependencies (expect, rclone, fuse3)
        id: step_deps
        continue-on-error: true
        run: |
          # 1. 在后台（&）异步启动 rclone 官方脚本下载并安装，不占用主线程时间
          echo "🚀 [Rclone] 后台开始下载与安装..."
          (sudo curl -fsSL https://rclone.org/install.sh | sudo bash > /tmp/rclone_install.log 2>&1 &)
          RCLONE_PID=$! # 记录 Rclone 任务的进程 ID
          
          # 2. 主线程同时利用多线程（-o APT::Keep-Downloaded-Packages=true）极速下载并安装系统依赖
          echo "📦 [Apt] 开始安装系统依赖 (expect, fuse3)..."
          sudo apt-get install -y -q --no-install-recommends \
            -o Acquire::ForceIPv4=true \
            expect fuse3
          
          # 3. 等待 Rclone 后台任务结束
          echo "⏳ 正在等待 Rclone 异步安装完成..."
          wait $RCLONE_PID
          
          # 4. 检查 Rclone 安装结果，若失败则打印日志
          if [ $? -ne 0 ]; then
            echo "❌ Rclone 安装失败，错误日志如下："
            cat /tmp/rclone_install.log
            exit 1
          else
            echo "✅ 所有依赖（expect, rclone, fuse3）全部并行安装成功！"
          fi
       
      # === 依赖安装（重试步骤，仅在失败时触发） ===
      - name: Install Dependencies (unzip, rclone, fuse3) - Retry
        if: steps.step_deps.outcome == 'failure'
        run: |
          # 备用方案：如果上面失败，走传统的 apt-get 强行安装
          sudo apt-get update
          sudo apt-get install -y rclone fuse3 expect
         
         
          
       # === 挂载 s3（原步骤） ===
      - name: Mount s3
        id: step_mount
        continue-on-error: true
        env:
          RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."

          sudo curl -L "$RCLONECONF" -o ./rclone.conf
          
          if [ -s ./rclone.conf ]; then
            echo "rclone.conf文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令
            
            sudo mkdir -p /home/runner/.config/rclone
            sudo mkdir -p /root/.config/rclone
           
            sudo cp ./rclone.conf /home/runner/.config/rclone/rclone.conf
            
            sudo cp ./rclone.conf /root/.config/rclone/rclone.conf
            
            #sleep 3
            
            #sudo rm -r /alist/r2
            
            sudo mkdir -p /alist
            
            sudo mkdir -p /r3
            
            sudo rclone mount r2:action /r3 --cache-dir /tmp --vfs-cache-mode full  --allow-other  --daemon
            
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 1
          fi  
          
          sudo rm -f ./rclone.conf

      # === 挂载 s3（重试步骤，仅在失败时触发） ===
      - name: Mount s3 - Retry
        if: steps.step_mount.outcome == 'failure'
        env:
          RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."

          sudo curl -L "$RCLONECONF" -o ./rclone.conf
          
          if [ -s ./rclone.conf ]; then
            echo "rclone.conf文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令
            
            sudo mkdir -p /home/runner/.config/rclone
            sudo mkdir -p /root/.config/rclone
           
            sudo cp ./rclone.conf /home/runner/.config/rclone/rclone.conf
            
            sudo cp ./rclone.conf /root/.config/rclone/rclone.conf
            
            #sleep 3
            
            #sudo rm -r /alist/r2
            
            sudo mkdir -p /alist
            
            sudo mkdir -p /r3
            
            sudo rclone mount r2:action /r3 --cache-dir /tmp --vfs-cache-mode full  --allow-other  --daemon
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 1
          fi  
          
          sudo rm -f ./rclone.conf
          
          

      # === 拉取镜像（原步骤） ===
      - name: Pull Docker Images
        id: step_pull
        continue-on-error: true
        run: |
          echo "=== 运行期状态调试 ==="
          echo "通过 env 接收到的缓存状态为: '$MY_CACHE_HIT'"
          
          echo "正在拉取 Docker 镜像..."
          
          cd /r3/back
          
          docker compose pull

      # === 拉取镜像（重试步骤，仅在失败时触发） ===
      - name: Pull Docker Images - Retry
        if: steps.step_pull.outcome == 'failure'
        run: |
          echo "=== 运行期状态调试 ==="
          echo "通过 env 接收到的缓存状态为: '$MY_CACHE_HIT'"
          
          echo "正在拉取 Docker 镜像..."
          
          cd /r3/back
          
          docker compose pull
     
       
      # 3. 启动所有 Docker 容器（原步骤）
      - name: Start Services and  Tunnel
        id: step_start_srv
        continue-on-error: true
        env:
          CF_TUNNEL_TOKEN: ${{ secrets.CF_TUNNEL_TOKEN }}
        run: |
          docker compose up -d
          
          sleep 2 
          
          docker stop cloudflared-tunnel
          
          echo "正在等待所有服务完成基础初始化..."
          #sleep 5 # 已经有了后面的死循环检测，这里可以把死等的 15s 缩短到 5s 节约时间


      # 3. 启动所有 Docker 容器（重试步骤，仅在失败时触发）
      - name: Start Services and  Tunnel - Retry
        if: steps.step_start_srv.outcome == 'failure'
        env:
          CF_TUNNEL_TOKEN: ${{ secrets.CF_TUNNEL_TOKEN }}
        run: |
          docker compose up -d
          
          sleep 2 
          
          docker stop cloudflared-tunnel
          
          echo "正在等待所有服务完成基础初始化..."
          #sleep 5 # 已经有了后面的死循环检测，这里可以把死等的 15s 缩短到 5s 节约时间


          
      # === 安装 1Panel（原步骤） ===
      - name: Install 1Panel
        id: step_1panel
        continue-on-error: true
        run: |
          # 1. 告诉 1Panel 脚本：Docker 已经安装好了，跳过 Docker 安装逻辑
          export SKIP_DOCKER_INSTALL="true"
    
          # 2. 提前设定面板参数，防止脚本卡在交互式输入界面
          export PANEL_PORT="26627"
          export PANEL_ENTRANCE="ec3ec2eabe"
          export PANEL_USERNAME="32ac1597da"
          export PANEL_PASSWORD="fb107bf8e4" # 建议换成你的安全密码
    
          # 3. 使用非交互模式运行官方脚本（加入无视终端检查）
          curl -sSL https://resource.fit2cloud.com/1panel/package/quick_start.sh | sudo -E bash

          
          
      # === 安装 1Panel（重试步骤，仅在失败时触发） ===
      - name: Install 1Panel - Retry
        if: steps.step_1panel.outcome == 'failure'
        run: |
          # 1. 告诉 1Panel 脚本：Docker 已经安装好了，跳过 Docker 安装逻辑
          export SKIP_DOCKER_INSTALL="true"
    
          # 2. 提前设定面板参数，防止脚本卡在交互式输入界面
          export PANEL_PORT="26627"
          export PANEL_ENTRANCE="ec3ec2eabe"
          export PANEL_USERNAME="32ac1597da"
          export PANEL_PASSWORD="fb107bf8e4" # 建议换成你的安全密码
    
          # 3. 使用非交互模式运行官方脚本（加入无视终端检查）
          curl -sSL https://resource.fit2cloud.com/1panel/package/quick_start.sh | sudo -E bash


      # === 挂载 s3（原步骤） ===
      - name: Mount s3
        id: step_mount_s3
        continue-on-error: true
        env:
          RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."

          sudo curl -L "$RCLONECONF" -o ./rclone.conf
          
          if [ -s ./rclone.conf ]; then
            echo "rclone.conf文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令
            
            sudo mkdir -p /home/runner/.config/rclone
            sudo mkdir -p /root/.config/rclone
           
            sudo cp ./rclone.conf /home/runner/.config/rclone/rclone.conf
            
            sudo cp ./rclone.conf /root/.config/rclone/rclone.conf
            
            #sleep 3
            
            #sudo rm -r /alist/r2
            
            sudo mkdir -p /alist
            
            sudo mkdir -p /r2
            
            sudo rclone mount r2:action /r2 --cache-dir /tmp --vfs-cache-mode full  --allow-other  --daemon
            
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 1
          fi  
          
          sudo rm -f ./rclone.conf

      # === 挂载 s3（重试步骤，仅在失败时触发） ===
      - name: Mount s3 - Retry
        if: steps.step_mount_s3.outcome == 'failure'
        env:
          RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."

          sudo curl -L "$RCLONECONF" -o ./rclone.conf
          
          if [ -s ./rclone.conf ]; then
            echo "rclone.conf文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令
            
            sudo mkdir -p /home/runner/.config/rclone
            sudo mkdir -p /root/.config/rclone
           
            sudo cp ./rclone.conf /home/runner/.config/rclone/rclone.conf
            
            sudo cp ./rclone.conf /root/.config/rclone/rclone.conf
            
            #sleep 3
            
            #sudo rm -r /alist/r2
            
            sudo mkdir -p /alist
            
            sudo mkdir -p /r2
            
            sudo rclone mount r2:action /r2 --cache-dir /tmp --vfs-cache-mode full  --allow-other  --daemon
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 1
          fi  
          
          sudo rm -f ./rclone.conf
          
 
      # 4. 下载物理 data.zip 备份并无损解压还原（原步骤）
      - name: Restore alist
        id: step_restore_alist
        continue-on-error: true
        env:
          #DATA_URL: ${{ secrets.DATA_URL }}
          #LOGIN_PASS: ${{ secrets.PASS }}
          #CF_TUNNEL_TOKEN: ${{ secrets.CF_TUNNEL_TOKEN }}
          DATA_PASS: ${{ secrets.DATA_PASS }}
        run: |
          sleep 2
          echo "正在从远程 URL 下载加密的物理备份压缩包..."
          #curl -L "$DATA_URL" -o ./data.zip
          sudo cp /r2/back/data.zip ./
          
          if [ -s ./data.zip ]; then
            echo "备份文件下载成功，正在执行覆盖解压..."
            
            docker stop alist
            
            echo "3. 清理宿主机空挂载目录，并直接解压物理备份到挂载点..."
            sudo rm -rf /home/alist-data
            #sudo mkdir -p /home/alist-data
            
            # 使用前面已经安装/缓存好的解压命令
            sudo unzip -P "$DATA_PASS" ./data.zip -d /home
            
            sudo chmod -R 777 /home/alist-data
            
            echo "5. 重新拉起 AList 容器，无缝加载刚刚恢复的物理数据库..."
            
            docker start alist
            
            echo "6. 强制同步最终登录管理员密码..."
            #docker exec alist /opt/alist/alist admin set "$LOGIN_PASS"
            echo "配置物理恢复完成！"         
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            
          fi
          
          sudo rm -f ./data.zip

      # 4. 下载物理 data.zip 备份并无损解压还原（重试步骤，仅在失败时触发）
      - name: Restore alist - Retry
        if: steps.step_restore_alist.outcome == 'failure'
        env:
          #DATA_URL: ${{ secrets.DATA_URL }}
          #LOGIN_PASS: ${{ secrets.PASS }}
          #CF_TUNNEL_TOKEN: ${{ secrets.CF_TUNNEL_TOKEN }}
          DATA_PASS: ${{ secrets.DATA_PASS }}
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."
          #curl -L "$DATA_URL" -o ./data.zip
          sudo cp /r2/back/data.zip ./
          
          if [ -s ./data.zip ]; then
            echo "备份文件下载成功，正在执行覆盖解压..."
            
            docker stop alist
            
            echo "3. 清理宿主机空挂载目录，并直接解压物理备份到挂载点..."
            sudo rm -rf /home/alist-data
            #sudo mkdir -p /home/alist-data
            
            # 使用前面已经安装/缓存好的解压命令
            sudo unzip -P "$DATA_PASS" ./data.zip -d /home
            
            sudo chmod -R 777 /home/alist-data
            
            echo "5. 重新拉起 AList 容器，无缝加载刚刚恢复的物理数据库..."
            
            docker start alist
            
            echo "6. 强制同步最终登录管理员密码..."
            #docker exec alist /opt/alist/alist admin set "$LOGIN_PASS"
            echo "配置物理恢复完成！"         
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            
          fi
          
          sudo rm -f ./data.zip

         
      
      # === 恢复 1Panel（原步骤） ===
      - name: Restore 1Panel
        id: step_restore_1panel
        continue-on-error: true
        
        run: |
        
          sudo curl -fsSL https://tailscale.com/install.sh | sh > tailscale.log 2>&1 &
                  
                   
          
            
          echo "正在从远程 URL 下载加密的物理备份压缩包..."
          
          sudo mkdir -p 1panel
          
          sudo cp /r2/back/panel.zip ./
          
          if [ -s ./panel.zip ]; then
            echo "panel文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令
            sudo 1pctl stop 
            
            sudo unzip -P "$DATA_PASS" ./panel.zip -d ./1panel
           
            sudo chmod -R 777 ./1panel
           
            sudo rm -rf /opt/1panel
            
            sudo cp -r ./1panel /opt
            
            sudo 1pctl start
            
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 1
          fi  
          
          sudo cp /r2/back/a.sh /opt/1panel
          
          sudo chmod 777 /opt/1panel/a.sh
          
          sudo rm -f ./panel.zip
          
          wait
          
          cat tailscale.log
          

      # === 恢复 1Panel（重试步骤，仅在失败时触发） ===
      - name: Restore 1Panel - Retry
        if: steps.step_restore_1panel.outcome == 'failure'
        run: |
        
          sudo curl -fsSL https://tailscale.com/install.sh | sh > tailscale.log 2>&1 &
        
        
        
          echo "正在从远程 URL 下载加密的物理备份压缩包..."

          sudo cp /r2/back/panel.zip ./
          
          if [ -s ./panel.zip ]; then
            echo "panel文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令
            sudo 1pctl stop 
            
            sudo unzip -P "$DATA_PASS" ./panel.zip -d ./
           
            sudo chmod -R 777 ./1panel
           
            sudo rm -rf /opt/1panel
            
            sudo cp -r ./1panel /opt
            
            sudo 1pctl start
            
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 1
          fi  
          
                    
          sudo cp /r2/back/a.sh /opt/1panel
          
          sudo chmod 777 /opt/1panel/a.sh
          
          sudo rm -f ./panel.zip
          
          
          wait
          
          cat tailscale.log



       # === 挂载 alist（原步骤） ===
      - name: Mount alist
        id: step_mount_alist
        continue-on-error: true
        #env:
          #RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
            sudo rclone mount mohic:/ /alist --cache-dir /tmp --vfs-cache-mode full  --allow-other  --daemon
            
      # === 挂载 alist（重试步骤，仅在失败时触发） ===
      - name: Mount alist - Retry
        if: steps.step_mount_alist.outcome == 'failure'
        #env:
          #RCLONECONF: ${{ secrets.RCLONECONF }}
        run: |
            sudo rclone mount mohic:/ /alist --cache-dir /tmp --vfs-cache-mode full  --allow-other  --daemon
            
         


      # === 配置 SSHD（原步骤） ===
      - name: Enable SSHD and set root password
        id: step_sshd
        continue-on-error: true
        run: |
          export MY_VAR=$(cat /r2/back/tmp)
          echo "root:$MY_VAR" | sudo chpasswd
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh
          
      # === 配置 SSHD（重试步骤，仅在失败时触发） ===
      - name: Enable SSHD and set root password - Retry
        if: steps.step_sshd.outcome == 'failure'
        run: |
          export MY_VAR=$(cat /r2/back/tmp)
          echo "root:$MY_VAR" | sudo chpasswd
          sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh
          
       
          

      # ==========================================
      # 10-13. 批量自动恢复并启动 1Panel 所有应用（精准一级目录版）
      # ==========================================
      - name: Auto Discover and Install 1Panel Apps
        id: step_auto_apps
        continue-on-error: true
        run: |
        
          echo "=================================================="
          echo "🔍 开始扫描 1Panel 一级应用目录..."
          echo "=================================================="
          
          # 检查 1Panel 应用根目录是否存在
          if [ ! -d "/opt/1panel/apps" ]; then
            echo "⚠️ 未找到 /opt/1panel/apps 目录，跳过自动扫描。"
            exit 0
          fi

          # 遍历下一级应用目录（例如 /opt/1panel/apps/mariadb）
          for app_dir in /opt/1panel/apps/*; do
            # 确保它是一个目录，并且旗下直接存在 docker-compose.yml
            if [ -d "$app_dir" ] && [ -f "$app_dir/docker-compose.yml" ]; then
              app_name=$(basename "$app_dir")
              
              echo "=================================================="
              echo "🚀 发现应用: [$app_name]，正在进入目录: $app_dir"
              echo "正在从远程 URL 下载加密的物理备份压缩包..." # 保留你原本的提示词
              echo "文件验证成功，正在拉起 Docker 容器..."
              echo "=================================================="
              
              # 进入该一级目录并启动容器
              cd "$app_dir"
              
              
              docker compose up -d || true
              
              
            fi
          done
          echo "🎉 所有检测到的 1Panel 应用已尝试启动完成！"
          
          
          echo "=================================================="
          echo "⚠️ 正在执行 1Panel 应用批量启动重试..."
          echo "=================================================="
          
          for app_dir in /opt/1panel/apps/*; do
            if [ -d "$app_dir" ] && [ -f "$app_dir/docker-compose.yml" ]; then
              echo "🔄 [重试] 正在拉起目录下的容器: $app_dir"
              app_name=$(basename "$app_dir")
              
              cd "$app_dir"
              
              docker compose up -d > log.txt 2>&1 || true
              
              if grep -qiE "Running" log.txt; then
              
                if grep -qiE "Error|failed" log.txt; then
                
                    echo "❌$app_name 启动失败" 
                
                    sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【应用】$app_name 启动失败！请检查 YML 配置。${{ github.repository }}")"

                else
                
                    echo "✅$app_name 启动成功" 
                
                fi
              
              else 
              
                echo "❌$app_name 启动失败" 
                
                sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【应用】$app_name 启动失败！请检查 YML 配置。${{ github.repository }}")"


              fi
              
            fi
          done
          

          
          

      # === 批量自动启动（重试步骤，仅在失败时触发） ===
      - name: Auto Discover and Install 1Panel Apps - Retry
        if: steps.step_auto_apps.outcome == 'failure1'
        id: step_auto_apps_retry
        continue-on-error: true
        run: |
          echo "=================================================="
          echo "⚠️ 正在执行 1Panel 应用批量启动重试..."
          echo "=================================================="
          
          for app_dir in /opt/1panel/apps/*; do
            if [ -d "$app_dir" ] && [ -f "$app_dir/docker-compose.yml" ]; then
              echo "🔄 [重试] 正在拉起目录下的容器: $app_dir"
              app_name=$(basename "$app_dir")
              
              cd "$app_dir"
              
              docker compose up -d > log.txt 2>&1 || true
              
              if grep -qiE "Running" log.txt; then
              
                if grep -qiE "Error|failed" log.txt; then
                
                    echo "❌$app_name 启动失败" 
                
                    sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【应用】$app_name 启动失败！请检查 YML 配置。${{ github.repository }}")"

                else
                
                    echo "✅$app_name 启动成功" 
                
                fi
              
              else 
              
                echo "❌$app_name 启动失败" 
                
                sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【应用】$app_name 启动失败！请检查 YML 配置。${{ github.repository }}")"


              fi
              
            fi
          done
          
 
 
      # === 恢复文件（原步骤） ===      
      - name: Restore file
        id: step_restore_file
        continue-on-error: true
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."
          
          sudo mkdir -p root
          
          sudo cp /r2/back/root.zip ./
          
          if [ -s ./root.zip ]; then
            echo "root文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令 
            
            sudo unzip -P "$DATA_PASS" ./root.zip -d ./root
           
            #sudo chmod -R 777 ./root
           
            
            sudo cp -r ./root/ /
            
            
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 0
          fi  
          
          
          sudo rm -f ./root.zip
          
          
      # === 恢复文件（重试步骤，仅在失败时触发） ===
      - name: Restore file - Retry
        if: steps.step_restore_file.outcome == 'failure'
        run: |
          echo "正在从远程 URL 下载加密的物理备份压缩包..."
          
          sudo mkdir -p root
          
          sudo cp /r2/back/root.zip ./
          
          if [ -s ./root.zip ]; then
            echo "root文件下载成功，正在执行覆盖解压..."
            
            # 使用前面已经安装/缓存好的解压命令 
            
            sudo unzip -P "$DATA_PASS" ./root.zip -d ./root
           
            #sudo chmod -R 777 ./root
           
            
            sudo cp -r ./root/ /
            
            
            
          else
            echo "错误：加密备份文件下载失败或为空，无法执行恢复！"
            exit 0
          fi  
          
          
          sudo rm -f ./root.zip
                   
       
      - name: Configure 1Panel admin
        run: |
          #sudo 1pctl user-info
          #echo "$USER" | sudo 1pctl update username
          #sudo expect -c "spawn 1pctl update password; expect \"Update panel password:\"; send \"${LOGIN_PASS}\r\"; expect \"Confirm password:\"; send \"${LOGIN_PASS}\r\"; expect eof"
          #echo "20333" | sudo 1pctl update port
          
  
    
    
      # === 安装 Tailscale（原步骤） ===      
      - name: Install tailscale
        id: step_tailscale
        continue-on-error: true
        env:
          TAIL: ${{ secrets.TAIL }}
        run: |
          echo "正在从远程 URL 安装Tailscale..."
          sudo mkdir -p /var/lib/tailscale
          
          sudo cp /alist/r2/tailscaled.state  /var/lib/tailscale
          
          sudo curl -fsSL https://tailscale.com/install.sh | sh
          
          sudo nohup tailscaled --state=/var/lib/tailscale/tailscaled.state >/dev/null 2>&1 &
         
          # sudo tailscale up --authkey=$TAIL
          #sleep 3


      # === 安装 Tailscale（重试步骤，仅在失败时触发） ===      
      - name: Install tailscale - Retry
        if: steps.step_tailscale.outcome == 'failure'
        env:
          TAIL: ${{ secrets.TAIL }}
        run: |
          echo "正在从远程 URL 安装Tailscale..."
          sudo mkdir -p /var/lib/tailscale
          
          sudo cp /alist/r2/tailscaled.state  /var/lib/tailscale
          
          
          
          sudo nohup tailscaled --state=/var/lib/tailscale/tailscaled.state >/dev/null 2>&1 &
         
          # sudo tailscale up --authkey=$TAIL
          #sleep 3


      - name: Delay start tailscale
        id: Delay_start
        continue-on-error: true

        run: |

          echo "🔍 正在计算从【任务真正开始】至今的时间..."
          echo "=================================================="
          
          # 🌟 改为读取第一步记录的绝对真实时间戳
          START_TIME=$REAL_START_TIME
          CURRENT_TIME=$(date +%s)
          ELAPSED=$((CURRENT_TIME - START_TIME))
          TARGET_WAIT=100 # 3分钟 = 180秒
          
          if [ $ELAPSED -lt $TARGET_WAIT ]; then
            WAIT_TIME=$((TARGET_WAIT - ELAPSED))
            echo "⏰ 任务实际只运行了 ${ELAPSED} 秒，未满 3 分钟。"
            echo "⏳ 还需要原地等待 ${WAIT_TIME} 秒..."
            echo "=================================================="
            sleep $WAIT_TIME
          else
            echo "✅ 任务从真正开始到现在已过去 ${ELAPSED} 秒（已超 3 分钟），无需等待！"
            echo "=================================================="
          fi          


          echo "Running tailscale up..."
         
          #stop other
          echo "stop other on successful"
          curl $(cat /alist/stop)
          
          docker start cloudflared-tunnel
          
          sudo tailscale up \
            --authkey=${{ secrets.TAIL }} \
            --hostname=github-actions-runner \
            --accept-routes \
            --accept-dns=false
          
          #sudo tailscale status

      - name: failure start
        if: steps.Delay_start.outcome == 'failure'
        
        run: |
        
          sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【启动失败】任务启动失败！请检查 YML 配置。${{ github.repository }}")"
          
          exit 1


          
        # 6. 保持工作流运行
      #- name: Restore Immich
        #run: |
          #echo "恢复immich..."
          #sudo gunzip -c $(ls -t /alist/immich/backups/*.sql.gz | head -n 1) | docker exec -i immich_postgres psql -U postgres -d immich
          #docker restart immich_server
                   
      # 6. 保持工作流运行
      - name: Keep Workflow Alive
        id: Keep_start
        continue-on-error: true
        run: |
          echo "=================================================="
          echo "【极速启动成功】所有服务、网络及物理挂载均已就绪！"
          echo "=================================================="
          
          # 1. 设置时间（5小时30分钟 = 19800）
          DURATION=19800
           
          #DURATION=300 
          
          START_TIME=$(date -u +%s)
          END_TIME=$((START_TIME + DURATION))
          
          #echo "工作流启动时间 (UTC): $(date -u -d @$START_TIME '+%Y-%m-%d %H:%M:%S')"
          
          echo "工作流启动时间 (UTC): $(TZ='Asia/Shanghai' date -d @$START_TIME '+%Y-%m-%d %H:%M:%S')"
          
          echo "预计结束时间 (UTC): $(TZ='Asia/Shanghai' date -d @$END_TIME '+%Y-%m-%d %H:%M:%S')"
             
          
          sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "预计结束时间 (UTC): $(TZ='Asia/Shanghai' date -d @$END_TIME '+%Y-%m-%d %H:%M:%S')，${{ github.repository }}")"
          
          
          echo "=================================================="
          sudo ip addr add 192.168.5.1/24 dev eth0
          
      

          while true; do
            CURRENT_TIME=$(date -u +%s)
            
            # 如果当前时间超过或等于预计结束时间，则退出循环
            if [ $CURRENT_TIME -ge $END_TIME ]; then
              echo "=================================================="
              echo "即将退出..."
              echo "=================================================="
             

              break
            fi
            
            sleep 30
          done
          
          
      - name: failure keep
        if: steps.Keep_start.outcome == 'failure'
        
        run: |
        
          sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【启动失败】任务启动失败！请检查 YML 配置。${{ github.repository }}")"
          
        
      # 2. 核心：只有当工作流被 Cancel 时，才会触发这个步骤
      - name: Auto Backup on EndTime
        if: steps.Keep_start.outcome == 'success'
        
        run: |
        
          sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【开始备份】切勿再写入数据！！！${{ github.repository }}")"

          sleep 60
        
          # 通过 -w 获取 HTTP 状态码，并用 -s 隐藏进度条
          HTTP_STATUS=$(curl -s -o response.json -w "%{http_code}" -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "$GH_URL" \
            -d '{"event_type": "mksha_trigger"}')

          echo "GitHub API 响应状态码: $HTTP_STATUS"
              
          # GitHub 触发成功的标准状态码是 204
          if [ "$HTTP_STATUS" -eq 204 ]; then
            echo "=================================================="
            echo "🎉【触发成功】API 请求已成功送达，目标工作流已启动！"
            
          
            sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "​✅【触发成功】API 请求已成功送达，目标工作流已启动！${{ github.repository }}")"
            
                
            echo "=================================================="
         
            cat response.json 2>/dev/null || true
          else
            echo "=================================================="
            echo "❌【触发失败】API 请求返回异常！请检查 Token 或 URL 配置。"
            echo "错误响应内容如下："
                echo "=================================================="
            cat response.json
               # 触发失败时，强行中断并让当前 Action 报错显红
               
            sleep 30
               
            HTTP_STATUS=$(curl -s -o response.json -w "%{http_code}" -X POST \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer $TOKEN" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "$GH_URL" \
              -d '{"event_type": "mksha_trigger"}')

            if [ "$HTTP_STATUS" -eq 204 ]; then
                
              echo "🎉【触发成功】API 请求已成功送达，目标工作流已启动！" 
                  
              sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "​✅【触发成功】API 请求已成功送达，目标工作流已启动！${{ github.repository }}")"
                  
            else 
                
              echo "❌【触发失败】API 请求返回异常！请检查 Token 或 URL 配置。"
                  
              sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "$(echo "❌【触发失败】API 请求返回异常！请检查 Token 或 URL 配置。${{ github.repository }}")"
  
                
            fi
                
          fi
              
    
          sudo bash /opt/1panel/task/all.sh


      - name: Wait to close
        id: wait
        run: |
        
          sudo python3 /opt/1panel/task/stop.py
          
    
          
       # 2. 核心：只有当工作流被 Cancel 时，才会触发这个步骤
      - name: Auto Backup on Cancel
        if: cancelled() && steps.Delay_start.outcome == 'success'
        
        run: |
          echo "检测到工作流被取消，正在紧急执行备份..."
          
          # 获取当前北京时间作为备份标签
          BACKUP_TIME=$(TZ='Asia/Shanghai' date '+%Y-%m-%d_%H%M%S')
          echo "备份触发时间: $BACKUP_TIME"
          
          # 执行你的备份脚本（传入你前面测试过的北京时间参数）
          # 注意：请确保 /r2/back/ 等路径或依赖在 Runner 中是存在的，或者改用你的实际备份命令
          
          sudo bash /opt/1panel/task/all.sh
          
          sudo bash /opt/1panel/a.sh $(cat /r2/back/ul.txt) $EPASS $(cat /r2/back/to.txt) "工作流被取消，紧急备份时间: $BACKUP_TIME，${{ github.repository }}"
          
          #sudo python3 /opt/1panel/task/stop.py